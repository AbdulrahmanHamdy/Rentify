# Common

Cross-cutting Application-layer building blocks (e.g. a `Result<T>` wrapper, pagination
request/response types, custom exceptions). Added incrementally as the phases that need
them are implemented.
